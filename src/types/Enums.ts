export enum ProtocolType {
    GRPC = 0,
    REST_API = 1
}

export enum RequestName {
    GET = 10,
    SET
}

export enum RequestDirection {
    FROM = 20,
    TO
}

export enum StreamType {
    READER = 30,
    WRITER
}
